/* eslint-disable no-unused-vars */
export enum ENUM_USER_ROLE {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  CANDIDATE = 'candidate',
  COMPANY = 'company',
}

export enum ENUM_USER_ACCOUNT_STATUS {
  ACTIVE = 'active',
  IN_ACTIVE = 'in_active',
  BLOCKED = 'blocked',
}
